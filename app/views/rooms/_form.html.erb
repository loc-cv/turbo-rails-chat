<%= form_with(model: room, class: "flex gap-2") do |f| %>
  <%= f.text_field :name, class: "w-[80%] rounded text-gray-800", autocomplete: "off" %>
  <%= f.submit "Create Room", class: "px-4 py-2 bg-green-100 text-gray-800 font-medium hover:bg-green-200 cursor-pointer rounded" %>
<% end %>
